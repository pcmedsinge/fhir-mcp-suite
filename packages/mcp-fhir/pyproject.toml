[project]
name = "mcp-fhir"
version = "0.1.0"
description = "FHIR R4 MCP server with built-in HAPI profile validation"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "fhir-mcp-suite contributors" }]
keywords = ["mcp", "fhir", "hl7", "healthcare", "ai"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]
dependencies = [
    "mcp[cli]>=1.6",
    "httpx>=0.27",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "structlog>=24.4",
    "fhir-mcp-shared",
]

[project.scripts]
mcp-fhir = "mcp_fhir.server:main"

[project.urls]
Homepage = "https://github.com/pcmedsinge/fhir-mcp-suite"
Repository = "https://github.com/pcmedsinge/fhir-mcp-suite"
"Bug Tracker" = "https://github.com/pcmedsinge/fhir-mcp-suite/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_fhir"]

[tool.uv.sources]
fhir-mcp-shared = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "respx>=0.21",
]
