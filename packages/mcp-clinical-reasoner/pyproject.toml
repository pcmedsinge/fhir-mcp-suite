[project]
name = "mcp-clinical-reasoner"
version = "0.1.0"
description = "Rule-based drug interactions, dose check, and allergy MCP server (RxNav backend)"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "fhir-mcp-suite contributors" }]
keywords = ["mcp", "clinical", "drug-interactions", "rxnav", "allergy", "healthcare"]
classifiers = [
    "Development Status :: 1 - Planning",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]
dependencies = [
    "mcp[cli]>=1.6",
    "httpx>=0.27",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "structlog>=24.4",
    "fhir-mcp-shared",
]

[project.scripts]
mcp-clinical-reasoner = "mcp_clinical_reasoner.server:main"

[project.urls]
Homepage = "https://github.com/pcmedsinge/fhir-mcp-suite"
Repository = "https://github.com/pcmedsinge/fhir-mcp-suite"
"Bug Tracker" = "https://github.com/pcmedsinge/fhir-mcp-suite/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_clinical_reasoner"]

[tool.uv.sources]
fhir-mcp-shared = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "respx>=0.21",
]
